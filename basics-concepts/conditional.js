// Condicionales

const uno   = 12;
const otro  = 20;

if(uno < otro) {
    console.log(`${uno} es menor a ${otro}.`);
} else if(uno === otro){
    console.log(`${uno} es igual a ${otro}.`);
} else {
    console.log(`${uno} es mayor a ${otro}.`);
}